<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Layout de Luuh</title>
  <style>
    /* Reset básico */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      height: 100vh;
      background-color: #fefefe;
    }

    /* Barra superior */
    .app-bar {
      background-color: #fde051; /* Cor do tema Flutter */
      color: black;
      text-align: center;
      padding: 1rem;
      font-size: 1.5rem;
      font-weight: bold;
    }

    /* Conteúdo principal */
    .content {
      flex: 1;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }

    /* Seções */
    .section {
      text-align: center;
      padding: 2rem;
      font-size: 1.2rem;
      font-weight: bold;
    }

    .middle-section {
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* Caixa central com estilo */
    .middle-box {
      background-color: #fc75b6; /* Cor do botão flutuante */
      padding: 2rem;
      border-radius: 20px;
      box-shadow: 5px 5px 10px rgba(0, 0, 0, 0.3);
      color: white;
      font-size: 1.5rem;
    }

    /* Botão flutuante */
    .fab {
      position: fixed;
      bottom: 1rem;
      right: 1rem;
      background-color: #fc75b6;
      color: white;
      border: none;
      border-radius: 50%;
      width: 60px;
      height: 60px;
      font-size: 24px;
      cursor: pointer;
      display: flex;
      justify-content: center;
      align-items: center;
      box-shadow: 3px 3px 6px rgba(0, 0, 0, 0.4);
    }

    .fab:hover {
      background-color: #f2549c; /* Efeito ao passar o mouse */
    }
  </style>
</head>
<body>
  <!-- Barra superior -->
  <div class="app-bar">Programa Layout</div>

  <!-- Conteúdo principal -->
  <div class="content">
    <!-- Seção superior -->
    <div class="section">Layout Superior</div>

    <!-- Seção do meio -->
    <div class="middle-section">
      <div class="middle-box">Meu Aplicativo</div>
    </div>

    <!-- Seção inferior -->
    <div class="section">Layout Inferior</div>
  </div>

  <!-- Botão flutuante -->
  <button class="fab">+</button>
</body>
</html>
